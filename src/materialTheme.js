import { createTheme } from "@mui/material";

export const customTheme = createTheme({
  palette: {
    primary: {
      // violeta principal
      main: "#9575CD",
      contrastText: "#EEE2DE",
    },
  },
  secondary: {
    // letras
    main: "#EEE2DE",
  },
});
